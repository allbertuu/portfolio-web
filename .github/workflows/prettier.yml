name: Code formatting

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check-out repo
        uses: actions/checkout@v4

      - name: Install deps
        run: npm install

      - name: Format code
        run: npm run format
